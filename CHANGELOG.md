# Changelog

All notable changes to this repository are documented here.

## v0.1.1

- **SDK**: stabilize v0.1 convenience methods (`inspect`, `resolve`, `enforce`, `supersede`) and align scope/override behavior with the contracts spec.
- **Docs**: update install + quickstart to use `pip install continuum-sdk`.
- **Examples**: add root `examples/flagship-demo/flagship_demo.py` wrapper for the 5-minute demo run.

## v0.1.0

- Initial OSS draft release (contracts + deterministic SDK + stubs for MCP/integrations).

