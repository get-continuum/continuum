# Slack copilot (minimal)

This is a minimal “Slack copilot” style example that runs **without a backend** by using `continuum-local`.

## Run

```bash
python3 -m venv .venv
. .venv/bin/activate
pip install -U pip
pip install continuum-local

python3 app.py
```

