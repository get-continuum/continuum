name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: pip install jsonschema pydantic pytest ruff mypy typer types-jsonschema "mcp>=1.0"

      - name: Install packages
        run: |
          pip install -e oss/sdk/python
          pip install -e oss/cli
          pip install -e oss/mcp-server
          pip install -e oss/integrations/llamaindex

      - name: Check OSS boundary
        run: bash scripts/check-boundary.sh

      - name: Lint with ruff
        run: ruff check oss/

      - name: Type-check SDK
        run: mypy oss/sdk/python/src/continuum/

      - name: Run tests
        run: pytest oss/contracts/tests/ oss/sdk/python/tests/ oss/cli/tests/ oss/mcp-server/tests/ oss/integrations/llamaindex/tests/ -v

  # The BSL-licensed engine lives in the separate `continuum-core` repository.
