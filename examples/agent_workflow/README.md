# Agent workflow (minimal)

Minimal agent-style loop using `continuum-local` (no backend required).

## Run

```bash
python3 -m venv .venv
. .venv/bin/activate
pip install -U pip
pip install continuum-local

python3 app.py
```

